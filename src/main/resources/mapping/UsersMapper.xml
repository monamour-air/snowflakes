<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.sun.stone.freelancer.mapper.UsersMapper">
	<resultMap id="UsersMap"
		type="com.sun.stone.freelancer.bean.UsersEntity">
		<result column="id" property="id" />
		<result column="username" property="username" />
		<result column="password" property="password" />
		<result column="enabled" property="enabled" />
		<result column="role_priv" property="rolePriv" />
	</resultMap>

	<!-- <select id="findAllUsers"
		resultType="com.sun.stone.freelancer.bean.UsersEntity">
		select * from users;
	</select> -->
	
	<select id="queryUserById" resultType="com.sun.stone.freelancer.bean.UsersEntity">
		SELECT * FROM users t WHERE 1=1 AND t.id = #{id}
	</select>
	
</mapper>